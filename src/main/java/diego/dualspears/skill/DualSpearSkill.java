package diego.dualspears.skill;

import yesman.epicfight.skill.Skill;
import yesman.epicfight.skill.passive.PassiveSkill;

public class DualSpearSkill extends PassiveSkill {
    public DualSpearSkill(Skill.Builder<? extends Skill> builder) {
        super(builder);
    }
}
